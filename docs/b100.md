# Entidad: B100 — Balance mensual por subcuenta

> **Implementado:** `true`

Entidad dedicada a B100. Una fila por subcuenta y mes: el dinero de cada subcuenta B100 en cada mes, junto con el dinero y porcentaje reservados para Hacienda.

## Datos que guarda

Todos los campos son `NOT NULL` salvo los de auditoría.

| Campo | Tipo | Descripción |
|---|---|---|
| `id` | VARCHAR(50) PK | Clave natural `{tipo}-{AAAA-MM}` |
| `tipo_subcuenta` | ENUM (`save`, `health`) | Tipo de subcuenta B100 |
| `mes` | VARCHAR(7) | Fecha mes-año en **una sola columna** (formato `YYYY-MM`, p. ej. `2026-01`) |
| `balance_mensual` | NUMERIC(12,2) | Dinero de esa subcuenta en el mes |
| `aporte_mensual` | NUMERIC(12,2) | Aporte que se hace ese mes |
| `dinero_hacienda` | NUMERIC(12,2) | Dinero apartado para Hacienda |
| `dinero_total_repartir` | NUMERIC(12,2) | Dinero total que reparte B100 al 100% |
| `porcentaje_hacienda` | NUMERIC(5,2) | Porcentaje que se lleva Hacienda (entero, p. ej. 19.00; se divide entre 100 para operar) |
| `fecha_creacion` / `fecha_actualizacion` | TIMESTAMP | Auditoría |

## Identificador

Fila única por `(tipo_subcuenta, mes)` ⇒ **clave natural legible `{tipo}-{AAAA-MM}`** (p. ej. `save-2026-07`, `health-2026-07`).

- Resta leer en logs y URLs (`/b100-balances/save-2026-07`).
- Inmutable por construcción (deriva de campos que no cambian).
- Compatible con la convención `VARCHAR(50)` del proyecto.
- Se refuerza con `UNIQUE(tipo_subcuenta, mes)` como red de seguridad.

**Decidido:** identificador natural `{tipo}-{AAAA-MM}`.

## Esquema de la tabla

```sql
CREATE TABLE b100_balances (
  id                   VARCHAR(50)   PRIMARY KEY,
  tipo_subcuenta       VARCHAR(20)   NOT NULL CHECK (tipo_subcuenta IN ('save', 'health')),
  mes                  VARCHAR(7)    NOT NULL,
  balance_mensual      NUMERIC(12,2) NOT NULL,
  aporte_mensual       NUMERIC(12,2) NOT NULL DEFAULT 0,
  dinero_hacienda      NUMERIC(12,2) NOT NULL DEFAULT 0,
  dinero_total_repartir NUMERIC(12,2) NOT NULL,
  porcentaje_hacienda  NUMERIC(5,2)  NOT NULL DEFAULT 19.00,
  fecha_creacion       TIMESTAMP     DEFAULT NOW(),
  fecha_actualizacion  TIMESTAMP     DEFAULT NOW(),
  UNIQUE(tipo_subcuenta, mes)
);

CREATE INDEX idx_b100_balances_mes ON b100_balances(mes);

COMMENT ON COLUMN b100_balances.id IS 'Clave natural {tipo}-{AAAA-MM} (p. ej. save-2026-07)';
COMMENT ON COLUMN b100_balances.tipo_subcuenta IS 'Tipo de subcuenta B100: save o health';
COMMENT ON COLUMN b100_balances.mes IS 'Fecha mes-año en una sola columna (YYYY-MM, p. ej. 2026-01)';
COMMENT ON COLUMN b100_balances.balance_mensual IS 'Dinero de esa subcuenta en el mes';
COMMENT ON COLUMN b100_balances.aporte_mensual IS 'Aporte que se hace ese mes';
COMMENT ON COLUMN b100_balances.dinero_hacienda IS 'Dinero apartado para Hacienda';
COMMENT ON COLUMN b100_balances.dinero_total_repartir IS 'Dinero total que reparte B100 al 100%';
COMMENT ON COLUMN b100_balances.porcentaje_hacienda IS 'Porcentaje que se lleva Hacienda (entero, p. ej. 19.00; se divide entre 100)';
COMMENT ON COLUMN b100_balances.fecha_creacion IS 'Auditoría: fecha de creación';
COMMENT ON COLUMN b100_balances.fecha_actualizacion IS 'Auditoría: fecha de última actualización';
```

## Endpoints

### Guardar datos de una subcuenta (POST)

Un único endpoint para guardar el balance de una subcuenta en un mes. El front lo llama **una vez por subcuenta** (`POST` para `save` y otro `POST` para `health`).

| Método | Path | Body | Uso |
|---|---|---|---|
| POST | `/b100-balances` | `B100BalanceRequest` (tipo_subcuenta, mes, balance_mensual, aporte_mensual, dinero_hacienda, dinero_total_repartir, porcentaje_hacienda) | Crear o actualizar el balance de una subcuenta en un mes (upsert por `(tipo_subcuenta, mes)`) |

### Listar balances (GET, paginado)

Obtener la lista de balances de una subcuenta, ordenada y con un número máximo de registros (estilo paginado por límite/orden).

| Método | Path | Query params | Uso |
|---|---|---|---|
| GET | `/b100-balances` | `tipo_subcuenta` (obligatorio), `limit` (nº registros, opcional con default), `order` (`asc`/`desc` por `mes`, opcional) | Listar los N últimos/pimeros meses de una subcuenta |

### Actualizar un balance (PUT)

Actualizar los campos de un balance existente, localizado por su id (`{tipo}-{AAAA-MM}`).

| Método | Path | Body | Uso |
|---|---|---|---|
| PUT | `/b100-balances/{id}` | `B100BalanceRequest` (campos editables) | Actualizar el balance de una subcuenta en un mes (`{tipo}-{AAAA-MM}`) |

### Eliminar un balance (DELETE)

Eliminar un balance existente, localizado por su id (`{tipo}-{AAAA-MM}`).

| Método | Path | Uso |
|---|---|---|
| DELETE | `/b100-balances/{id}` | Eliminar el balance de una subcuenta en un mes (`{tipo}-{AAAA-MM}`) |

## Reglas de diseño

- Enum de dominio: `TipoSubcuentaB100 { save, health }` en `domain/enums/`.
- Modelo de dominio: `B100Balance` (`id, tipoSubcuenta, mes, balanceMensual, aporteMensual, dineroHacienda, dineroTotalRepartir, porcentajeHacienda` + fechas), con `mes` de tipo `String` (`YYYY-MM`).
- Entidad JPA: `B100BalanceMO` (`@Table(name = "b100_balances")`).
- Sin FK a `cuentas`: el tipo de subcuenta es propio de B100, no reutiliza el catálogo genérico.
- Patrón hexagonal del proyecto, como `InteresAnualMintos` o `BalanceFondo`.

## Capas de implementación previstas

- **Capa de aplicación**: `B100Balance` (domain model), `TipoSubcuentaB100` (enum), `B100BalanceDrivingPort`, `B100BalanceUseCase`, `B100BalanceDrivenPort`.
- **Capa driven**: `B100BalanceMO`, `B100BalanceRepository`, `B100BalanceDatasourceAdapter`, `B100BalanceDatasourceMapper`.
- **Capa driving**: controlador implementando `B100BalanceApi` (swagger), `B100BalanceApiMapper`.
- **Swagger**: nuevos schemas y paths para la entidad b100 (CRUD de `B100Balance`).
- **Flyway**: nueva migración `V17__create_b100_balances.sql`.
- **Tests**: ampliación de `B100IT` (o `B100EntityIT`) para la nueva entidad.
