1- Vamos a empezar de 0 con las llamadas al back. Comenta todas las llamadas y vamos desbloqueando poco a poco



(cambiar el swagger por el nuevo)
2- activa la llamada a operationID getSnapshots que permite obtener las nominas. En la pantalla de nominas se debe llamar a ese endpoint para comprobar si existe alguna
En caso de existir obtener los valores y cargarlos en 

